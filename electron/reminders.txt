how to gradient: background-image: linear-gradient(180deg, var(--stBlue), var(--black));
starting app:
- npm start (for manual refresh)
- npx electronmon . (for responsive changes with automatic refresh)